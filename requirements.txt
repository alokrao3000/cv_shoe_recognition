# Web service (mirrors sneaker-arbitrage's stack for consistency)
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.17    # required by FastAPI for file-upload endpoints
pydantic-settings==2.6.1
python-dotenv==1.0.1
httpx==0.28.1

# CV / embeddings
torch==2.5.1                # CPU by default; auto-uses CUDA if a GPU build is installed
transformers==4.46.3        # DINOv2 model + image processor
pillow==12.3.0
pillow-heif==1.7.0          # HEIC/HEIF support — default photo format on iPhone
numpy==1.26.4

# Reading the sneaker-arbitrage Postgres DB to source reference images
# (read-only; this project keeps no database of its own)
psycopg2-binary==2.9.10

# Stealth browser for scripts/backfill_stockx_images.py — see
# app/browser_session.py's docstring for why (StockX's API has no image
# field; its website is behind Cloudflare bot management). patchright, not
# stock Playwright — pinned to match sneaker-arbitrage's proven version.
# After installing, run: python -m patchright install chromium
patchright==1.61.2
setuptools<70   # patchright pulls pyee>=13; see sneaker-arbitrage/requirements.txt

# Tests
pytest==8.3.3
