[pytest]
markers =
    slow: downloads model weights / does real inference — excluded by default
    integration: needs the project's Postgres (docker compose up -d postgres); self-skips when unreachable
addopts = -m "not slow"
